#include "teacher.h"

Teacher::Teacher() {
    // constructor stub
}

/* Load the HTML template from a file. */
void Teacher::load_template() {
    // stub
}

/* Apply student data to the HTML file. This does not return anything, but writes and saves
 * a file to be downloaded by the client.
 */
void Teacher::run_template(std::vector<std::vector<int>> students_scores) {
    // stub
    return;
}
